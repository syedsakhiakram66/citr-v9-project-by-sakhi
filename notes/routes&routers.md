Routers like TanStack or React Router, make client side routing preety cool. They help us do many things that can't be done in plain apps. Most importantly letting us build a SPA.

Lazy Loading is a technique of only loading the requested route when required. If you do not use lazy loading your app can be slow.

In the routes file you just .lazy in the file name

